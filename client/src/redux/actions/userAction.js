import { USERLOADED } from './type';
import { API, setAuthToken } from '../../config/api';

// export const userLoaded = (token) =>{
//   return {
//     type : USERLOADED,
//     payload :
//   }
// }
